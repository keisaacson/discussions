<ul class="nav nav-tabs" role="tablist">
  <li><%= link_to 'Leader Page', "/discussions/#{@discussion.id}/leader" %></li>
  <li class="active"><%= link_to 'Participant Page', discussion_path(@discussion.id) %></li>
</ul>

<div class="row">
	<div class="col col-xs-12">
		<h2><%= @discussion.title %></h2>
		<p class="message-container"></p>
	</div>
</div>

<div class="row">
	<div class="col col-xs-12">
		<div class="row">
			<div class="col col-xs-12">
				<%= render('questions/new_question_form', {:discussion => @discussion}) %>
			</div>
		</div>

		<% surveys = @discussion.surveys.group_by {|s| s.survey_status} %>
		<% questions = @discussion.questions.group_by {|q| q.question_status} %>

		<div class="row">
			<div class="col col-sm-6">
				<div class="surveys-container">
					<h4>Open Surveys</h4>
					<ul class="survey-list">
						<% if surveys['open'] %>
							<% surveys['open'].each do |survey| %>
								<%= render('survey_responses/survey_response_form', {:survey_id => survey.id, :survey_question => survey.survey_question}) %>
							<% end %>	
						<% end %>
					</ul>
				</div>
			</div>

			<div class="col col-sm-6">
				<div class="answered-questions-container">
					<h4>Questions w/ Answers</h4>
					<ul class="answered-questions-list">
						<% if questions['answered'] %>
							<% questions['answered'].each do |question| %>
								<li><%= question.content %></li>
								<p><%= question.response %></p>
							<% end %>	
						<% end %>
					</ul>
				</div>
			</div>
		</div>
	</div>
</div>







